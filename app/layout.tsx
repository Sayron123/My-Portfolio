import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "next-themes";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://sayron-portfolio.sayrondpabilando.workers.dev"),
  title: "Sayron Pabilando — Software Developer",
  description: "Portfolio of Sayron Pabilando — projects, skills, and experience in web development.",
  openGraph: {
    title: "Sayron Pabilando — Software Developer",
    description: "Projects, skills, and experience.",
    url: "https://sayron-portfolio.sayrondpabilando.workers.dev",
    siteName: "Sayron Pabilando Portfolio",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Sayron Pabilando — Software Developer",
    description: "Projects, skills, and experience.",
    images: ["/og-image.png"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}